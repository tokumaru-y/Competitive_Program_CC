#include <bits/stdc++.h>
using namespace std;
int main () {
    int n,w;
    scanf("%d %d",&n,&w);
    long long dp[n][w] ={0};
    long long ll[n][2];
    for(int i=0;i<n;i++){
        int a,b;
        scanf("%d %d",&a,&b);
        ll[i][0]=a;ll[i][1]=b;
    }
    for(int i=0;i<n;i++){
        for(int j=0;j<w;j++){
            if(j-ll[n][0]>=0){
                dp[]
            }
        }
    }
}
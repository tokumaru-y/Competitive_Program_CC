#include <bits/stdc++.h>
using namespace std;

int main() {
    int a,b;
    scanf("%d %d", &a,&b);
    if(a > 8 || b >8) {
        printf("%s", ":(");
    } else {
        printf("%s", "Yay!");
    }
}